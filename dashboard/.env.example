# Mycelial Dashboard Configuration
# Copy this file to .env.local and adjust values for your environment

# ===========================================
# P2P Network Configuration
# ===========================================

# WebSocket URL for real-time P2P network updates
# Connects to mycelial-node at port 8080
VITE_P2P_WS_URL=ws://localhost:8080/ws

# REST API URL for P2P peer queries
# Endpoints: /api/peers, /api/info, /api/stats, /health
VITE_P2P_API_URL=http://localhost:8080

# ===========================================
# Orchestrator Configuration
# ===========================================

# Orchestrator WebSocket URL for real-time events
# Connects to mycelial-orchestrator at port 9090
VITE_ORCHESTRATOR_WS_URL=ws://localhost:9090/api/v1/events

# Orchestrator REST API URL
# Endpoints: /api/v1/nodes, /api/v1/workloads, /api/v1/cluster/status
VITE_ORCHESTRATOR_API_URL=http://localhost:9090

# ===========================================
# Security Configuration
# ===========================================

# Ed25519 Request Signing:
# All API requests include the following signed headers for authentication:
# - X-Public-Key: Your Ed25519 public key (peer identity)
# - X-Signature: Ed25519 signature of the canonical request
# - X-Timestamp: Request timestamp (prevents replay attacks)
# - X-Nonce: Random nonce (ensures request uniqueness)
#
# Keys are automatically generated and stored in localStorage:
# - mycelial_ed25519_private_key
# - mycelial_ed25519_public_key

# ===========================================
# Development Options
# ===========================================

# Enable mock data mode for development (true/false or 1/0)
# When enabled, the dashboard will use simulated data instead of real API
# Mock data will also auto-enable after 3 consecutive API failures as fallback
VITE_USE_MOCK_DATA=true

# ===========================================
# CORS Configuration
# ===========================================

# API requests are sent with CORS mode enabled:
# - mode: 'cors'
# - credentials: 'omit' (no cookies for cross-origin)
# - Content-Type: 'application/json'
#
# Your backend must allow these headers:
# - Access-Control-Allow-Origin: * (or specific origin)
# - Access-Control-Allow-Headers: Content-Type, X-Public-Key, X-Signature, X-Timestamp, X-Nonce
# - Access-Control-Allow-Methods: GET, POST, OPTIONS
